/**
 * Placeholder — detailed AuthContext coverage lives in
 * __tests__/unit/contexts/AuthContext.test.tsx
 */
describe('AuthContext - integration placeholder', () => {
  it('keeps the suite non-empty', () => {
    expect(true).toBe(true);
  });
});
